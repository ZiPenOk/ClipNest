services:
  clipnest:
    build: .
    image: clipnest:dev
    volumes:
      - ./app:/app/app
      - ./tools:/app/tools
      - ./data:/data
      - ${CLIPNEST_DOWNLOAD_DIR_HOST:-./downloads}:/downloads
